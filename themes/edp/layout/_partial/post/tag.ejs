<% if (post.tags && post.tags.length){ %>
    <%
    var tags = [];
    post.tags.forEach(function(tag){
        tags.push('<a href="' + config.root + tag.path + '">' + tag.name + '</a>');
    });
    %>
    <span class="article-tags"><i class="fa fa-tag"></i> <%- tags.join(', ') %></span>
<% } %>
